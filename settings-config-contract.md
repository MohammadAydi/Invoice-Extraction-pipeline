# Settings / Pipeline Configuration Contract — moved

The one copy of this contract now lives at
[../docs/settings-config-contract.md](../docs/settings-config-contract.md), beside the API
contract it is the sibling of.
