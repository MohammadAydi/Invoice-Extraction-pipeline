# --- core pipeline -------------------------------------------------------
pydantic>=2.0
PyYAML>=6.0
numpy>=1.24
opencv-python>=4.8
matplotlib>=3.7

# --- HTTP service --------------------------------------------------------
# The desktop app talks to `api.main:app`; see docs/api-contract.md.
fastapi>=0.115
uvicorn[standard]>=0.34
pydantic-settings>=2.7
python-multipart>=0.0.20

# --- default OCR engine --------------------------------------------------
# Tesseract is the light default: no weights, no GPU, no second virtualenv.
# It also needs the Tesseract *binary* plus its `ara` and `eng` traineddata,
# which pip cannot install -- see README.md.
pytesseract>=0.3.13
Pillow>=10.0

# --- optional OCR engines ------------------------------------------------
# Install these only to use the `surya`, `qwen_vlm` or `surya_qwen` engines.
# `ocr/engines/__init__.py` skips whichever of them will not import, so the
# service starts without any of them.
#
#   pip install surya-ocr==0.17.1 "transformers<5.0"

surya-ocr==0.17.1
transformers<5.0

# The `surya_qwen` engine additionally runs the VLM in a *second* virtualenv
# with transformers>=5.0; its path is configured under
# `ocr.engine_params.worker_python` in config/qwen_config.yaml.
