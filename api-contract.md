# API Contract — moved

The one copy of this contract now lives at [../docs/api-contract.md](../docs/api-contract.md),
beside the settings contract and the database schema. The version that used to be here was
several revisions behind and described a response shape neither side had produced for some
time.
