[pytest]
testpaths = tests
# The pipeline's packages are imported as top-level names (config, core, ocr,
# ...), so the project root has to be on sys.path when pytest collects.
pythonpath = .
addopts = -q
